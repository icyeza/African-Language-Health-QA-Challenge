# Core (EDA, preprocessing, evaluation, baselines) — CPU-only, free Colab
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=12.0
rouge-score>=0.1.2

# Fine-tuning (Experiment 2+) — GPU recommended
transformers>=4.40
datasets>=2.18
accelerate>=0.30
sentencepiece>=0.2
peft>=0.10          # LoRA / QLoRA (the "strong" config)
# bitsandbytes>=0.43  # uncomment for 4-bit QLoRA on the large/decoder config
